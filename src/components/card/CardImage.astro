---
import { Image } from 'astro:assets';

const { class: className, imageClass, width, height, alt, src } = Astro.props;
---

<div class={`card--image ${className && className}`}>
    <Image
        src={src}
        width={width ? width : 672}
        height={height ? height : 452}
        alt={alt}
        loading="lazy"
        format={"webp"}
        class={`object-cover rounded-2xl ${imageClass && imageClass}`}
    />
</div>
